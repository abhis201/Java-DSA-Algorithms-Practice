import java.util.ArrayList;
import java.util.List;
import java.util.stream.Stream;

public class test {
    
    //Stack, Queue, Hashmap, Linkedlist, ArrayList, Sets or List
    //StringBuffer - regex: "[ \\n\\t\\r.,;:!?(){]"
    //StreamsAPI, Lambda - later
    //Generics, Interface
    //Insertion Sort, Quicksort
    //Binary Search, Linear search
    //Abstraction, Encapsulation, Inheritance

    public static void revrow(List<List<Integer>> matrix){
        int n = matrix.size();
        System.out.println(n);
    }
    //print only 2 decimals System.out.print("%.2f",variable);
    public static void main(String[] args){

        String num = "12";
        String next = "" + (Integer.parseInt(num) + 10);

        System.out.println(next);
        System.out.println(num=="12");
        System.out.println(0^6);
        //List<List<Integer>> mat = new ArrayList();
        List<List<Integer>> mat = new ArrayList<>();
        mat.add(new ArrayList<Integer>(4));

       // mat.get(0).add(0,1);
        //mat.get(0).add(0,1);

        //System.out.println(mat.get(0).get(0)+", "+mat.get(0).get(1));

        System.out.println(3^1^3);

        int[][] graph = new int[4][2];
        List<List<Integer>> adjMat = new ArrayList<>();
        
    }

}


 